<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">    <!-- 1. BARRA SUPERIOR (Sin cambios) -->
    <LinearLayout
        android:id="@+id/barraSuperior"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageButton
            android:id="@+id/btn_atras"
            android:layout_width="42dp"
            android:layout_height="47dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Atrás"
            android:src="@drawable/regresar" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:gravity="center_vertical"
            android:minHeight="47dp"
            android:text="Sonido"
            android:textSize="20sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- 2. BOTÓN DE GRABAR (Usa el nombre exacto de tu imagen) -->
    <ImageButton
        android:id="@+id/btn_grabar"
        android:layout_width="186dp"
        android:layout_height="205dp"
        android:layout_centerInParent="true"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Grabar"
        android:scaleType="fitCenter"
        android:src="@drawable/rojo" /> <!-- ¡CORRECCIÓN CLAVE AQUÍ! -->

</RelativeLayout>




