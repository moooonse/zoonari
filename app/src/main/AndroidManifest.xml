<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zoonari"
        tools:targetApi="31">

        <!-- Aquí también corregimos el nombre de 'iniciosesion' a 'InicioSesionActivity' -->
        <activity
            android:name=".InicioSesionActivity"
            android:exported="false" />

        <activity
            android:name=".SonidoActivity"
            android:exported="false" />

        <!--
            === INICIO DE LA CORRECCIÓN ===
            Actualizamos el nombre para que coincida con el nombre de la clase en el archivo .kt
        -->
        <activity
            android:name=".PrincipalActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- === FIN DE LA CORRECCIÓN === -->

        <activity
            android:name=".SegundaPantallaActivity"
            android:exported="false" />

    </application>

</manifest>
